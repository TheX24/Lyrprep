{
  "name": "lyrprep",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --bun vite",
    "build": "bun run --bun vite build",
    "preview": "bun run --bun vite preview --host",
    "start": "bun run --bun ./server.ts",
    "lint": "bun run --bun tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "@types/pako": "^2.0.4",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@mux/mux-player": "^3.11.5",
    "@types/node": "^25.5.0",
    "hono": "^4.12.8",
    "lucide": "^0.562.0",
    "marked": "^17.0.4"
  }
}
