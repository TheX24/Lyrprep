{
  "name": "lyrprep",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --bun vite",
    "build": "bun run --bun vite build",
    "preview": "bun run --bun vite preview --host",
    "start": "bun run --bun ./server.ts",
    "lint": "bun run --bun tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/pako": "^2.0.4",
    "typescript": "^6.0.3",
    "vite": "^8.0.13"
  },
  "dependencies": {
    "@mux/mux-player": "^3.13.0",
    "@types/node": "^25.8.0",
    "hono": "^4.12.19",
    "lucide": "0.562.0",
    "marked": "^18.0.3"
  }
}
